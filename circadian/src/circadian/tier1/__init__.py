"""Tier 1: the phase-only additive oscillator -- the everyday engine, and the
"add a function to the base function" extension point. See
docs/circadian-model.md for when this (weak-coupling / linear-superposition)
approximation is valid and when to reach for `model_b` instead.
"""
